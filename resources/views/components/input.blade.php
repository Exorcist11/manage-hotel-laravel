<div class="px-2 py-2 rounded-lg {{ $class }}">
    <input type="{{ $type }}" name="{{ $name }}" value="{{ $value }}"
        placeholder="{{ $placeholder }}" class="outline-none w-full" required autofocus>
</div>
